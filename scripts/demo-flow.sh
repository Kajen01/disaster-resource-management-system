#!/usr/bin/env sh

echo "1. Register an ADMIN or SHELTER_MANAGER user through /api/auth/register"
echo "2. Create a shelter using /api/shelters"
echo "3. Intake a donation batch using /api/resources/batches"
echo "4. Create a shortage request using /api/shares/requests"
echo "5. Match, dispatch, and receive the transfer"
echo "6. Trace the donation through /api/transparency/donations/{donationRef}"
