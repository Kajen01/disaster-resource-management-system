input {
  beats {
    port => 5044
  }
}

filter {
  mutate {
    add_field => { "stack" => "drms" }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "drms-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
